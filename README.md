# Фінансовий Планувальник

PWA mobile-first платформа для обліку та планування фінансів з підтримкою мультикористувацьких груп та спільних бюджетів.

## 🚀 Деплой на GitHub Pages

Проект налаштований для автоматичного деплою на GitHub Pages через GitHub Actions.

### Налаштування

1. **Додати Secrets в GitHub:**
   - Перейдіть в Settings → Secrets and variables → Actions
   - Додайте:
     - `NEXT_PUBLIC_SUPABASE_URL` - URL вашого Supabase проекту
     - `NEXT_PUBLIC_SUPABASE_ANON_KEY` - Anon key з Supabase

2. **Увімкнути GitHub Pages:**
   - Settings → Pages
   - Source: **GitHub Actions**

3. **Push в main/master гілку:**
   - Автоматично запуститься workflow для деплою
   - Після успішного деплою сайт буде доступний на `https://username.github.io/repo-name`

### Локальний build для перевірки

```bash
npm run export
# Результат буде в папці /out
```

## Технологічний стек

### Core
- **Next.js 14** (App Router) - React framework з SSR/SSG
- **TypeScript** - типобезпека
- **React 18** - UI бібліотека

### UI & Styling
- **shadcn/ui** - компоненти на базі Radix UI
- **Tailwind CSS** - utility-first CSS
- **Framer Motion** - анімації
- **Lucide React** - іконки

### State & Data
- **Zustand** - легкий state management
- **Supabase** - хмарна база даних (PostgreSQL)
- **React Hook Form** + **Zod** - форми та валідація

### Features
- **next-intl** - мультимовність (українська/англійська)
- **next-pwa** - Progressive Web App
- **Recharts** - графіки та аналітика
- **date-fns** - робота з датами

## Встановлення

```bash
npm install
```

## Запуск проекту

```bash
# Розробка
npm run dev

# Production build
npm run build
npm start

# Static export (для GitHub Pages)
npm run export

# Лінтер
npm run lint
```

## Налаштування Supabase

Перед запуском потрібно налаштувати Supabase:

1. Створіть проект на https://supabase.com
2. Виконайте SQL схему з файлу `supabase-schema.sql` в Supabase Dashboard
3. Створіть файл `.env.local` з вашими ключами:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=your-project-url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   ```

Детальні інструкції в `SUPABASE_SETUP.md`

## Структура проекту

```
├── app/
│   ├── [locale]/          # Локалізовані маршрути
│   │   ├── login/         # Сторінка входу
│   │   ├── register/      # Сторінка реєстрації
│   │   └── page.tsx       # Лендінг
│   └── layout.tsx         # Root layout
├── components/
│   └── ui/                # shadcn компоненти
├── lib/
│   └── utils/             # Утиліти
├── messages/               # Файли перекладів
│   ├── uk.json
│   └── en.json
└── public/                # Статичні файли
```

## Функціональність

### Поточна реалізація
- ✅ Лендінг сторінка
- ✅ Сторінки реєстрації та входу
- ✅ Мультимовність (UK/EN)
- ✅ PWA налаштування
- ✅ Базові UI компоненти

### Планується
- 📝 Створення місячного бюджету
- 📝 Додавання транзакцій
- 📝 Аналітика витрат
- 📝 Групи користувачів
- 📝 Спільні бюджети
- 📝 Push-сповіщення

## Мультимовність

Підтримуються мови:
- Українська (uk) - за замовчуванням
- Англійська (en)

Переклади знаходяться в `messages/uk.json` та `messages/en.json`.

## PWA

Проект налаштований як Progressive Web App. Manifest файл знаходиться в `public/manifest.json`.

## Розробка

Проект використовує:
- **ESLint** для лінтингу
- **TypeScript** для типобезпеки
- **Tailwind CSS** для стилізації

## Ліцензія

Приватний проект

